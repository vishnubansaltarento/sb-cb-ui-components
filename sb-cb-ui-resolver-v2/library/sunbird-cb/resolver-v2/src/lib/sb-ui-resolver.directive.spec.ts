import { SbUiResolverDirective } from './sb-ui-resolver.directive'

describe('WidgetResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new SbUiResolverDirective()
    expect(directive).toBeTruthy()
  })
})
