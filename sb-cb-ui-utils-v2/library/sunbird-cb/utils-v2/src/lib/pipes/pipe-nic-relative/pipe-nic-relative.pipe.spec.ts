import { PipeNicRelativePipe } from './pipe-nic-relative.pipe'

describe('PipeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNicRelativePipe()
    expect(pipe).toBeTruthy()
  })
})
