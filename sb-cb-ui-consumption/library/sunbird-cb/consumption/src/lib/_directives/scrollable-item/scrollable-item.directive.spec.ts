import { ScrollableItemDirective } from './scrollable-item.directive';

describe('ScrollableItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollableItemDirective();
    expect(directive).toBeTruthy();
  });
});
